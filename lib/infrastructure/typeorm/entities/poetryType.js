class PoetryType {
    constructor({
                    id,
                    name,
                    pic
                } = {}) {
        this.id = id
        this.name = name
        this.pic = pic
    }
}

module.exports = PoetryType
